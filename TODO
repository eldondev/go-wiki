The Patches at the bottom still contain the entire diff.  It should be possible
to create a change from two TreeEntries, convert that change to a patch, and
thus display only the changes from that file.
